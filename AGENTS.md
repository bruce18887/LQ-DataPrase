# 项目代理指令（AGENTS.md）

本项目由 Claude Code 与 DSH（DeepSeek Harness）两款代理协作，本文件是两款代理**都会在会话开始时自动加载**的入口文件。项目规则与教训的唯一事实来源仍是下列原始文件，本文件只负责把两款代理引向它们，不复制正文（避免双份漂移）。

## 会话开始必读（两款代理相同）

1. **`.claude/CLAUDE.md`** — 项目工作流规则与硬性约束（计划模式 / 子代理策略 / lessons 迭代 / 完成前验证 / 任务管理 / 600 行限制 / e2e 要求 / 双主题维护等）。**必须完整阅读并遵守**。
2. **`docs/tasks/lessons.md`** — 历史教训库（约 109KB，禁止整体内联进指令文件）。开始工作前先复习**文首「通用高频规则 R1–R6」**与最近几个日期条目，再动手；开发中遇到疑似踩过的坑先回查对应条目。
3. **`docs/tasks/todo.md`** — 当前任务清单与进度（如存在）。

## 维护约定

- 规则正文只维护在 `.claude/CLAUDE.md`；教训只追加到 `docs/tasks/lessons.md`。
- 本文件保持最小，仅做入口指引；如两款代理的行为出现分歧，以本文件声明的“原始文件为唯一事实来源”为准。
每个项目会话开始时先读该项目根 AGENTS.md / CLAUDE.md
