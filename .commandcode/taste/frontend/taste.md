# Frontend preferences

- The user expects settings/config features (e.g. export/report timeout customization) to be surfaced with a visible UI item in the system settings page, and will follow up if it's missing. Do not ship a backend-only setting without a corresponding visible settings-page control. Confidence: 0.6